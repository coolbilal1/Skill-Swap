// pages/index.jsx
import React from "react";
import Link from "next/link";
import config from "../config.json";

export default function Home() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-gray-50">
      {/* Header */}
      <header className="w-full p-6 bg-[color:var(--primaryColor)] text-white text-center">
        <img src={config.logo} alt="Logo" className="mx-auto h-20 mb-4" />
        <h1 className="text-4xl font-bold">{config.siteName}</h1>
        <p className="mt-2 text-lg">{config.homepageText}</p>
      </header>

      {/* Main Buttons */}
      <main className="mt-16 flex flex-col items-center space-y-6">
        <Link href="/browse">
          <button className="px-8 py-4 bg-[color:var(--secondaryColor)] text-white rounded-lg text-xl hover:opacity-90">
            Browse Skills
          </button>
        </Link>
        <Link href="/post">
          <button className="px-8 py-4 bg-[color:var(--secondaryColor)] text-white rounded-lg text-xl hover:opacity-90">
            Offer a Skill
          </button>
        </Link>
        <Link href="/talk">
          <button className="px-8 py-4 bg-[color:var(--secondaryColor)] text-white rounded-lg text-xl hover:opacity-90">
            Video/Voice Chat
          </button>
        </Link>
        <Link href="/admin">
          <button className="px-8 py-4 bg-gray-700 text-white rounded-lg text-xl hover:opacity-90">
            Admin Panel
          </button>
        </Link>
      </main>

      {/* Footer */}
      <footer className="mt-auto w-full text-center p-4 bg-gray-100 text-gray-700">
        &copy; {new Date().getFullYear()} {config.siteName}. All rights reserved.
      </footer>
    </div>
  );
}